# Base dependencies (always installed)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
sounddevice>=0.4.6
numpy>=1.24.0
requests>=2.31.0
python-multipart>=0.0.6

# Speech recognition backends (installed by setup script based on hardware):
# GPU:  pip install faster-whisper>=1.0.0
# CPU:  pip install vosk
